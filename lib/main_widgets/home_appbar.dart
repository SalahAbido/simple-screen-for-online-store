import 'package:flutter/material.dart';

AppBar HomeAppBar() => AppBar(
    elevation: 0.0,
    backgroundColor: Colors.teal[300],
    title: const Text("OnLine Abido Store"),
    actions: [IconButton(onPressed: () {}, icon: const Icon(Icons.menu))],
  );
